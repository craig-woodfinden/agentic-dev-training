# L2.4 starter
- `compare_models.py` — runs your L2.3 eval set against Haiku, Sonnet, Opus.
- Outputs `model_comparison.csv` plus a summary line per model.
- The module asks you to compute cost-per-call from the pricing page and write a one-page memo recommending one model.
