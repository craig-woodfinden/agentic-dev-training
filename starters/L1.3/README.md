# L1.3 starters

- **CLAUDE.md.example** -- drop into the root of a real repo and edit. Keep it under one screen.
- **slash-command-branch-summary.md** -- save as `~/.claude/commands/branch-summary.md` to use via `/branch-summary` in Claude Code.

Both are scaffolds, not finished artefacts. Bend them to your repo.
