# L2.2 starter
- `repo_agent.py` -- tool-using agent with `list_files` + `read_file`.
- Usage: `python repo_agent.py /path/to/repo "your question"`.
- The module asks you to remove the safety checks ON PURPOSE to see what happens -- put them back before committing.
